export default defineAppConfig({
  ui: {
    colors: {
      primary: "amber",
      neutral: "slate",
    },
    button: {
      slots: {
        base: "justify-center",
        label: "text-center",
      },
    },
  },
});
